{
  "name": "planets",
  "version": "1.0.0",
  "description": "A generative, reactive Elementary Audio web example",
  "scripts": {
    "start": "parcel --no-cache index.html",
    "build": "rm -rf dist && parcel build --no-source-maps --no-cache --public-url . index.html",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "author": "Nick Thompson",
  "license": "ISC",
  "devDependencies": {
    "gh-pages": "^3.2.3",
    "parcel": "^2.2.1"
  },
  "dependencies": {
    "@elemaudio/core": "^1.0.0",
    "@elemaudio/srvb": "^1.1.1",
    "@elemaudio/web-renderer": "^1.0.0",
    "teoria": "^2.5.0"
  }
}
